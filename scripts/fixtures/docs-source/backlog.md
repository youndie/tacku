# Backlog: a lending system the staff can trust

> A fixture, and a repository of nobody's. It exists so the read-only view can be photographed the
> way every other screen is — from a body a real server produced — without that server having to
> reach a real forge. Its shape is the method's: one file per item in `backlog/`, this index above
> them.

## Stages

| Identifier | Stage |
|---|---|
| `stage-trust` | Making the ledger match the shelf |
| `stage-members` | What a member can do without asking staff |
