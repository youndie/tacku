# Набор токенов

Перенесено из `tacku Design Spec.dc.html` §1, чтобы набор жил в репозитории, а не в макете: по §6
спеки KOMPOT токены объявляет дизайн-система клиента и публикует обычными константами, а сервер
обязан использовать именно их. Пока набор существует только в макете, сервер и клиент расходятся
молча — токен вне набора приложение переживает подстановкой значения по умолчанию, то есть экран не
ломается, а **обесцвечивается**, и опечатка выглядит как решение дизайнера.

Этот файл — исходник для констант ([B-24](../backlog/B-24-design-tokens-artifact.md)), а не их
замена. Расхождение между ним и константами клиента чинится в константах.

Тема тёмная, окно 1440×900, 1 dp = 1 px.

## Два правила, без которых набор не читается

1. **Токен типографики несёт размер, начертание и цвет.** Отдельного цвета текста в протоколе нет:
   у `text` есть только `style`. Поэтому «красная надпись об ошибке» существует не как «текст +
   красный», а как токен `error`.
2. **Цветовой токен применяется только к фону и градиенту.** Это единственные два узла модификатора,
   принимающие цвет (`background`, `gradient`).

## Токены текста (13)

| Токен | Размер / начертание | Цвет | Роль |
|---|---|---|---|
| `display` | 28 / 600 | `#F2F3F5` | Заголовок экрана. Ровно один на экран. |
| `title` | 20 / 600 | `#F2F3F5` | Заголовок блока внутри экрана; название продукта в панели. |
| `subtitle` | 15 / 600 | `#9AA1AC` | Разделитель смысловых групп: день в ленте, секция карточки, заголовок колонки. **Заменяет рамку.** |
| `body` | 14 / 400 | `#E3E5E9` | Основной текст: названия задач, тексты событий, описание. |
| `body_muted` | 14 / 400 | `#9AA1AC` | Второстепенный: счётчики, подзаголовки, тело пустого состояния. |
| `value` | 14 / 500 | `#F2F3F5` | Значение в `read_only_field`; подпись выбранного пункта навигации. |
| `label` | 12 / 500 | `#8A919C` | Подпись поля ввода, подпись значения, placeholder. |
| `meta` | 12 / 400 | `#7C838E` | Время, идентификатор, срок — и авторство, когда действие сделал человек. |
| `meta_agent` | 12 / 500 | `#D9A254` | То же место, но действие сделал агент. Единственный цветной сигнал происхождения в тексте. |
| `button_primary` | 14 / 600 | `#FFFFFF` | Подпись кнопки на фоне `accent`. **См. оговорку ниже.** |
| `button_quiet` | 14 / 500 | `#A9B6E8` | Кнопка без фона: навигация, отмена, второстепенное. **См. оговорку ниже.** |
| `error` | 13 / 400 | `#F0908C` | Текст ошибки поля и текст отказа сервера. Строку пишет сервер. |
| `notice` | 13 / 500 | `#E3B665` | Только заглушка неизвестного элемента. Отдельный токен, чтобы её нельзя было спутать с ошибкой. |

**Оговорка про кнопки.** У `KompotComponentButton` нет поля стиля — `type`, `id`, `modifiers`,
`text`, `action`, и всё. Сервер не может выбрать, каким токеном набрана подпись. Два токена выше
остаются в наборе как **правило дизайн-системы**: подпись кнопки с фоном `accent` набирается
`button_primary`, без фона — `button_quiet`. Правило живёт в клиенте и на провод не выезжает.
Развилка разобрана в [сверке §1.1](design-review.md).

## Токены цвета (12)

Применяются **только** к `background` и `gradient`.

| Токен | HEX | Где применяется |
|---|---|---|
| `surface` | `#101114` | Фон окна и рабочей области. База, от которой отсчитываются остальные. |
| `surface_block` | `#191B1F` | Сгруппированный блок: панель навигации, колонка статуса, строка ленты. **Заменяет карточку с рамкой.** |
| `surface_field` | `#23262B` | Заливка поля ввода, `read_only_field`, карточки задачи внутри колонки. Он же статус «To do». |
| `surface_selected` | `#2C313A` | Текущий пункт навигации, текущая задача. Выделение делает фон, а не цвет подписи. |
| `accent` | `#4E6FE3` | Фон главного действия. Единственный насыщенный цвет интерфейса кроме `agent`. |
| `agent` | `#D08B2C` | Полоса 2 dp у всего, что сделал агент. **Больше нигде.** |
| `agent_quiet` | `#2E240F` | Фон блока, целиком относящегося к работе агента: сводка в шапке ленты. |
| `danger` | `#3B2124` | Заливка поля в ошибке и блока отказа по существу. Он же статус «Blocked». |
| `notice` | `#2C2717` | Фон заглушки неизвестного элемента. |
| `divider` | `#292D33` | Строка высотой 1 dp вместо рамки. И полоса-заглушка на месте `agent` у действий человека — чтобы строки ленты не разъезжались. |
| `status_active` | `#1D3346` | Фон значения статуса «в работе» и «в ревью». |
| `status_done` | `#1E3329` | Фон значения статуса «готово». |

**Отдельных токенов для «To do» и «Blocked» нет намеренно:** их роль уже несут `surface_field` и
`danger`. Каждое лишнее имя — ещё одно место, где сервер и приложение могут разойтись.

**`agent` — обещание, а не оттенок.** Как только он появится на декоративном элементе, сигнал
происхождения умрёт. Это стоит проверять автоматически, а не помнить
([B-24](../backlog/B-24-design-tokens-artifact.md)); различимость самого сигнала — предмет
[B-28](../backlog/B-28-agent-stripe-accessibility.md).

## Кто красит поле в ошибке

`danger` в таблице стоит первым делом «заливка поля в ошибке» — но красит его **клиент**, не сервер.
У `text_input` нет поля состояния; по §9.5 клиент подсвечивает `fieldId`, пришедший в ответе об
ошибке. Токен нужен, применяет его дизайн-система. Подробно — [сверка §1.2](design-review.md).

## Якоря

| Что | Где |
|---|---|
| источник набора | `tacku Design Spec.dc.html` §1 (проект Claude Design «Планирование структуры дизайна») |
| поля компонентов | `kompot-spec/schema/kompot-standard.schema.json`, `kompot-spec/schema/kompot-forms.schema.json` |
| норма про токены | `kompot-spec/SPEC.md` §6 |
