---
id: B-24
title: "Набор токенов как публикуемый артефакт клиента"
status: done
priority: P0
size: S
stage: s3-client
blocked_by: [B-18]
---

# B-24 — Набор токенов как публикуемый артефакт клиента

Макет отдал тринадцать текстовых и двенадцать цветовых токенов с описанием роли каждого. По §6
спеки набор объявляет дизайн-система клиента и публикует обычными константами, а сервер обязан
использовать именно их, а не выдумывать строки.

- **Пока набор живёт в макете, сервер и клиент расходятся молча.** Токен вне набора приложение
  переживает подстановкой значения по умолчанию — экран не ломается, а **обесцвечивается**, и
  опечатка выглядит как решение дизайнера. Единственная защита — константы, на которые ссылается
  серверный код.
- Правило, которое надо перенести вместе с набором: **цвет применяется только к фону и градиенту**,
  цвет текста живёт внутри токена типографики. Из него следует, что `danger` на поле в ошибке
  красит клиент, а не сервер (см. [сверку §1.2](../design/design-review.md)).
- Второе правило оттуда же: **`agent` не используется нигде, кроме полосы происхождения.** Как
  только он появится на декоративном элементе, сигнал умрёт. Это стоит проверять, а не помнить.
- Не покрывает: `button_primary`/`button_quiet`. У кнопки нет поля стиля, и как различаются подписи
  — решает дизайн-система клиента; развилка в [сверке §1.1](../design/design-review.md).

- AC: набор опубликован константами; серверный код ссылается на них, а не на строковые литералы;
  токен вне набора ловится проверкой, а не глазами.
- Anchors: `client/app/src/`, `docs/design/design-spec-tokens.md`.

**Закрыто 22.08.2026, и оказалось больше, чем учётом.**

**Дизайн-система теперь наша.** Клиент использовал `Material3DesignSystem` тулкита, а имена токенов у
нас свои — `surface_block`, `agent`, `meta_agent`. Чужая дизайн-система их не знает, и **каждое** имя
разрешалось бы в умолчание с предупреждением: экран рисуется, в неправильных цветах, и говорит об
этом в логе, куда никто не смотрит.

**Список публикуется машиночитаемо.** `TackuDesignSystem` печатает `design/tokens.json`, Go-сторона
держит против него свои константы. Проверено мутацией: переименование одной константы роняет тест
с обеих сторон — «сервер шлёт имя, которого клиент не знает» и «клиент объявил имя, которого никто не
шлёт».

**Правило про `agent` проверяется, а не помнится.** Тест обходит все деревья и требует, чтобы каждый
узел, покрашенный этим токеном, был полосой: три точки шириной, без детей. Мутация, красящая им
колонку, роняет тест обоими условиями сразу.

**И сторож от пустой проверки нашёл настоящий дефект.** Первый прогон сказал «ни один узел не
использует цвет агента» — потому что **карточки доски вообще не показывали происхождение**: полоса
там была захардкожена серой. Доска — экран, на который команда смотрит чаще всего, — оказалась
единственным местом, где центральное обещание продукта не выполнялось. Ничего не падало; сигнала
просто не было.
