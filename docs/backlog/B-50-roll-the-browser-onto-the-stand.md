---
id: B-50
title: "Выкатить браузерный клиент на стенд"
status: done
priority: P1
size: M
stage: s4-product
blocked_by: [B-49]
---

# B-50 — Выкатить браузерный клиент на стенд

[B-49](B-49-web-client.md) закрыт: страница собирается, сервер её отдаёт, вход через провайдера
проходит целиком на локальном стенде. Здесь — довести это до контура, и порядок шагов тут не
формальность.

## Что выяснилось про провайдера

Обе находки сделаны измерением живого провайдера, а не чтением документации.

**CORS нет вовсе.** `discovery` отвечает 200 без `Access-Control-Allow-Origin`, `OPTIONS` на `jwks`
и `token` — 404. Все браузерные клиенты провайдера до сих пор гоняли свою половину флоу на сервере,
поэтому запросы шли не из браузера и вопрос не вставал.

**`aud` не кладётся ни в один access-токен** — ни в браузерном флоу, ни в `client_credentials`;
`aud` есть только у `id_token`, и там это clientId. Наша проверка аудитории (§12, RFC 8707)
не может пройти ни для одного токена этого провайдера.

Из второго следует то, чего никто не знал: **агентская поверхность на стенде тоже не работала.**
Она развёрнута давно, но настоящий токен туда никто не приносил, а без обращения отказ неотличим от
исправной работы. Развёрнуто — не то же самое, что проверено.

Обе половины исправлены у провайдера — PR открыт, ссылка не приводится: этот репозиторий публичный
и соседей по имени не называет (`scripts/no_private_names.py`).

## Что уже сделано

| шаг | состояние |
|---|---|
| правка провайдера | влита и **выпущена**; проверено живой командой: discovery отдаёт `Access-Control-Allow-Origin` |
| наш PR | влит в `main` |
| образ `0.2.0` | опубликован в ghcr; собрался со второго раза — компилятор wasm не влезал в память раннера, куче назван размер |
| значения контура | ветка готова, PR намеренно не открыт |

## Что проверено на выкаченном стенде

Не «под поднялся» — под поднимался и раньше:

| | |
|---|---|
| страница | отдаётся, бандл и `/auth/config` отвечают; конфигурация приезжает от сервера, а не из бандла |
| редирект | браузер уходит к провайдеру и получает форму входа — значит, клиент **принят** |
| приём не формальный | проверено подменой: чужой адрес возврата → `invalid_request`, несуществующий клиент → `unauthorized_client`. Приём правильного клиента что-то значит только потому, что неправильный отвергается |
| экран без токена | 401 |
| метаданные защищённого ресурса | отвечают неаутентифицированно, как требует RFC 9728 |
| второй двери в релизе нет | опубликованный образ, запущенный без провайдера, отказывается стартовать словами «this build has no second door» — то есть форма входа в релизную сборку действительно не попала |

**Чего проверить не удалось и почему.** Самого входа человеком: учётные данные провайдера — не мои, и
вводить их я не буду. Отсюда же следует единственное оставшееся неизвестное — **есть ли у клиента
`tacku-web` аудитория** — адрес страницы с завершающим `/`. Если её не задали, обмен кода вернёт
`invalid_target`, и вход закончится отказом на последнем шаге. Это видно с первой же попытки войти.

## Агентская поверхность отвечает — впервые с момента развёртывания

Проверено `scripts/check_agent.sh` и продолжением вручную, на выкаченном стенде, настоящим токеном:

| | |
|---|---|
| токен | `azp: tacku-agent`, в `aud` — MCP-эндпоинт этого развёртывания; совпадает с тем, что сервер объявляет по RFC 9728 |
| `initialize` | `200`, сессия выдана |
| `tools/list` | 9 инструментов |
| `tools/call list_boards` | `{"boards":[{"id":"test","title":"test"}]}` — настоящие данные, а не пустой успех |

Последняя строка и есть ответ на вопрос, которого раньше никто не задавал: до этого поверхность
отвечала 401 всем — и когда была сломана, и когда была бы исправна.

## Что осталось

Обе операции у провайдера сделаны владельцем — доступа к порту управления у меня нет и не было.
Оставлено как есть для истории:

1. ~~публичный клиент `tacku-web`~~ — заведён, адрес возврата проверен подменой;
2. ~~аудитория агентскому клиенту~~ — задана, и это видно в claims выданного токена, а не только
   в том, что запрос прошёл.

Второе важно не меньше первого: без него агенты продолжат получать токены, которые мы отвергаем, —
ровно то, из-за чего агентская поверхность не работала всё это время.

## Порядок

Порядок обязателен: с 0.2.0 формы входа в релизной сборке нет, и выкатка раньше провайдера оставит
контур без входа вовсе — а не «с частично рабочим входом».

1. Наверху вливается и **выпускается** правка провайдера; версия артефактов доезжает до его
   развёртывания, миграция применяется.
2. В провайдере заводится публичный клиент: `tacku-web`, PKCE `S256`, адрес возврата — адрес
   страницы **с завершающим `/`** (сверяется точным совпадением), аудитория — он же.
3. Существующему агентскому клиенту добавляется аудитория MCP-эндпоинта — иначе агенты продолжат
   получать токены, которые мы отвергаем.
4. `youndie/tacku` PR #2 вливается, публикуется образ (версия берётся из чарта — 0.2.0), затем
   ветка значений контура.

## Чем проверять, что доехало

Не «под поднялся» — под поднимался и раньше:

| | |
|---|---|
| CORS | `curl -D- -H 'Origin: <адрес страницы>' <issuer>/.well-known/openid-configuration` → заголовок есть |
| `aud` | получить токен и посмотреть его claims: там должен быть адрес, а не пустота |
| страница | войти человеком в браузере и увидеть доску |
| агент | сделать вызов MCP настоящим токеном — то, чего не делали ни разу |

Последняя строка важнее остальных: именно её отсутствие спрятало вторую находку.
