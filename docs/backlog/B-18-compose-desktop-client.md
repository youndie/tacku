---
id: B-18
title: "Клиент на Compose Desktop поверх опубликованных модулей kompot"
status: done
priority: P0
size: L
stage: s3-client
blocked_by: [B-08]
---

# B-18 — Клиент на Compose Desktop поверх опубликованных модулей kompot

Приложение-прототип: окно на JVM, рендер дерева экрана готовыми модулями kompot
(`kompot-client` и рендереры плагинов формы и картинок), тема через `kompot-ds-material-compose`.
Своего кода — сборка, вход, конфигурация адреса и набор токенов дизайн-системы.

- **Клиент здесь — измерительный прибор, а не продукт.** Он самая обкатанная часть системы, поэтому
  расхождение, которое он показывает, почти наверняка на стороне сервера. Из этого следует
  дисциплина: не чинить клиент, чтобы «заработало», а сначала выяснять, кто из двоих неправ по
  спеке.
- Набор токенов объявляет клиент и публикует обычными константами (§6); сервер обязан использовать
  именно их, а не выдумывать строки. Значит набор появляется до экранов — см.
  [бриф дизайнера](../design/design-brief.md) §4.
- Клиент обязан пережить неизвестный тип компонента заглушкой (§2.1); заглушку надо нарисовать, а не
  оставить на усмотрение рендерера.
- Не покрывает: iOS и Android. Прототип десктопный.

- AC: приложение открывает доску и карточку задачи с живого Go-сервера; неизвестный тип компонента
  даёт заглушку, а не пустой экран; неизвестный токен даёт значение по умолчанию и предупреждение в
  лог.
- Anchors: `client/app/src/`, `client/app/build.gradle.kts`.

**Закрыто 21.08.2026.** Окно на Compose Desktop, дерево рисует тулкит, тема — Material 3 через
`Material3DesignSystem`. Своего кода — транспорт, навигация и обработка намерений; ни одного
рендерера, потому что рендерер, написанный здесь, был бы вторым мнением о том, как выглядит
компонент.

**Прибор сработал сразу.** `make probe` декодирует каждый экран **сериализаторами тулкита** — проверка
сильнее схемной: схема говорит, что допустимо, а это говорит, что примет код, который будет рисовать.
Первый же прогон упал дважды, на двух механизмах подряд — разбор см. [Q-19](../research/questions.md).

Сейчас проходит целиком: вход, замена сессии, граф из пяти маршрутов, все пять экранов (89 и 116
узлов у двух screen'ов), список с продолжением.

**Три поведения закреплены тестом**, а не абзацем: неизвестный компонент деградирует в заглушку и
экран живёт; неизвестный тип поля теряет **всю** форму и называет себя; неизвестный экшен деградирует
и **молча ничего не делает**.

Побочно всплыли две грабли потребителя: транзитивные типы провода приходят как `implementation` и
консьюмеру не видны, а репозиторий Google нужен с фильтром — нефильтрованный, став недоступным,
отравляет резолв целиком.
